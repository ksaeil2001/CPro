# ============================================================================
# REQUIRED CONFIGURATION
# ============================================================================

# Database (REQUIRED)
# PostgreSQL connection string for the application database
POSTGRES_USER=manga
POSTGRES_PASSWORD=CHANGE_ME_TO_STRONG_PASSWORD
POSTGRES_DB=manga_translator
DATABASE_URL=postgresql+asyncpg://manga:CHANGE_ME_TO_STRONG_PASSWORD@db:5432/manga_translator

# OpenAI API Key (REQUIRED for translation)
# Get your key from: https://platform.openai.com/api-keys
# Must start with 'sk-' and be at least 20 characters
OPENAI_API_KEY=sk-proj-your-actual-api-key-here

# ============================================================================
# OPTIONAL CONFIGURATION
# ============================================================================

# Google Vision (optional backup OCR)
# Only needed if you want Google Cloud Vision as backup OCR
# Leave commented out if not using
# GOOGLE_APPLICATION_CREDENTIALS=/app/credentials/gcloud.json

# Replicate (optional backup inpainting)
# Only needed if you want Replicate as backup inpainting service
# Leave commented out if not using
# REPLICATE_API_TOKEN=r8_your-token-here

# ============================================================================
# APPLICATION SETTINGS
# ============================================================================

# App
BACKEND_HOST=0.0.0.0
BACKEND_PORT=8000
FRONTEND_URL=http://localhost:3000
BACKEND_URL=http://backend:8000
LOG_LEVEL=INFO

# CORS - Comma-separated list of allowed origins
# For production, update this to your actual frontend URL
ALLOWED_ORIGINS=http://localhost:3000

# Cost limits (in Korean Won)
MAX_COST_PER_PAGE_KRW=10
DAILY_COST_LIMIT_KRW=10000

# Exchange rate (USD to KRW)
USD_KRW_RATE=1400
